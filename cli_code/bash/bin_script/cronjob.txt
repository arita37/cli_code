  GNU nano 2.5.3                                             File: /tmp/crontab.JHBVJU/crontab                                                                                                 

# Edit this file to introduce tasks to be run by cron.
 # For example, set a higher log level to audit cron's work
 EXTRA_OPTS="-L 1"


# @reboot sleep 5 && su ubuntu -c "nohup  /home/ubuntu/anaconda3/bin/jupyter notebook --ip=0.0.0.0 --port=8888 --no-browser  &>> /home/ubuntu/project27/logfile_jupyter_server.txt"


###Clean zlog folder
@reboot sleep 15  && nohup   /home/ubuntu/zbatch_clean.sh  &>> /home/ubuntu/zlog/cron_zbatch.log


### Jupyter
@reboot sleep 11  &&  nohup  /home/ubuntu/anaconda3/bin/jupyter notebook --ip=0.0.0.0 --port=8888 --no-browser  &>> /home/ubuntu/zlog/jupyter_server.log


## S3 drive
@reboot sleep 10  &&  /home/ubuntu/zconda/goofys ztasks /home/ubuntu/zs3drive



#### Daily pull test
0 1 * * *  /home/ubuntu/test/test_git.sh dev /home/ubuntu/test/aapackage/ /home/ubuntu/test/test_aapackage.sh 2>&1 >> /home/ubuntu/zlog/cron_gitpull.log


#### Backup home_ubuntu
0 2 * * *  /home/ubuntu/zscript/homepush 2>&1 >> /home/ubuntu/zlog/cron_homepush.log






#
# Each task to run has to be defined through a single line
# indicating with different fields when the task will be run
# and what command to run for the task
#
# To define the time you can provide concrete values for
# minute (m), hour (h), day of month (dom), month (mon),
# and day of week (dow) or use '*' in these fields (for 'any').#
# Notice that tasks will be started based on the cron's system
# daemon's notion of time and timezones.
#
# Output of the crontab jobs (including errors) is sent through
# email to the user the crontab file belongs to (unless redirected).
#
# For example, you can run a backup of all your user accounts
# at 5 a.m every week with:
# 0 5 * * 1 tar -zcf /var/backups/home.tgz /home/
#
# For more information see the manual pages of crontab(5) and cron(8)
#
# m h  dom mon dow   command